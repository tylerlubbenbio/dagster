# TEMPLATE: Copy this directory, rename (remove underscore prefix), and customize.
# This is the template source directory -- do not use directly.
#
# See _example_api_source/ and _example_db_source/ for working examples.

source: template_source
source_type: api  # or: internal_db
connection_resource: template_resource
custom_extractor: true
default_group: raw_template
default_tags:
  layer: raw
  source_type: api
  source_system: template
  schedule_tier: daily

assets: []
